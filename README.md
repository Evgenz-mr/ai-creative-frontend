# AI Creative Frontend

Next.js frontend for the AI Creative Director portfolio and Kubernetes/Argo CD lab.

## Local run

```bash
npm install
npm run dev
```

Environment:
- `NEXT_PUBLIC_API_URL` defaults to `http://localhost:8080`

The application includes a first editorial-style landing page, backend API integration, container build, CI and Helm deployment.
